# Dependências do projeto Data Major
# Instalação no Databricks via %pip install -r requirements.txt
# (ou pip install -r requirements.txt em ambiente local)

# Engenharia de dados / AWS
boto3>=1.34
awswrangler>=3.5
requests>=2.31
# pyarrow recente: lê Parquet gravado por Spark recente (evita o erro de Thrift LogicalType)
# No Databricks, instalar junto com numpy>=1.26,<2 (numpy 2 quebra o pandas do runtime)
pyarrow>=17,<25

# Mineração e estatística (motor é pandas; Spearman via pandas rank+corrwith, sem scipy)
scikit-learn>=1.4
pandas>=2.0
numpy>=1.24

# Visualização
matplotlib>=3.7
seaborn>=0.13

# Conector PostgreSQL (carga Load — opcional)
psycopg2-binary>=2.9
sqlalchemy>=2.0
